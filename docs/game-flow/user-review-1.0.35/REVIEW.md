# User review of 1.0.35 — 2026-09-17

Retrieved user notes and all seven latest screenshots from ChatGPT conversation 6aab0bb9-6178-83ed-9184-79fad0eabd9f. The user asked Codex to review these findings. This is analysis, not an implemented or installed update.

## Implementation follow-up: 1.0.36 (2026091704)

User subsequently requested completing the fixes and installing a new test version. Training countdown is now two seconds (animation speed 1.5x); challenge remains three seconds. Loading overlay fades in/out for 75 ms each. Pairs comparison keeps its input lock and half-second mismatch reveal without tinting the board; disabled-button tint equals normal tint. Flash header starts at the same visible anchored position used for recall. FullScreenArtwork expands full-screen Image geometry to the canvas edges while its child controls retain safe-area layout. Locked achievement badges use a UI grayscale shader supporting stencil/rect clipping; text stays readable, rows remain tappable, status prefixes are removed, and details show requirements without collection progress/reward UI.

Unity reviews and existing image/progress/achievement checks passed. Inspected menu, Pairs, Flash presentation and recall, locked/unlocked achievements and requirement popup. Version 1.0.36 (2026091704) was built, installed on ZogajSon and confirmed running after about 40 seconds. Pairs rapid-tap behavior and pacing still need human phone testing.

1. Transitions feel unnaturally abrupt. User requests approximately 100–200 ms of natural visual pacing. Prefer a short visual transition over reinstating multi-second loading sleeps.
2. Ready/Steady/Go should take about two seconds with an even race-start rhythm. Current GamesScript.ShowRSG explicitly runs training in one second with animator speed multiplied by three; this confirms the cause.
3. Pairs sometimes greys every card after the second selection. CompareCards calls MakeButtonsInteractable(false) and ChangeCardsColor(false). The latter tints every child, including already matched cards, while restoration touches only remaining buttons. Disabled button tint can add further dimming. Separate input blocking from visual appearance, preserving comparison delay and matched-card state. Code confirms a plausible direct cause; no new phone reproduction performed yet.
4. Full-screen backgrounds must extend to all screen edges. Only camera-sensitive controls should move inward. Current MobileSafeArea wraps entire screen containers, including nested backgrounds, producing blue/cyan bands visible in IMG_2166, IMG_2162, IMG_2163 and IMG_2164. Pairs/Flash level selection screenshots IMG_2165 and IMG_2167 demonstrate the desired edge-to-edge background.
5. Flash timer/header is clipped during image presentation (IMG_2159). Prior editor review only covered the recall state. Inspect both presentation and recall, including animation target positions and anchors; a generic safe-area wrapper alone did not resolve it.
6. Achievements: locked icons should be greyed but requirements remain readable; tapping shows how to earn the achievement, without an earned/reward presentation. Unlocked icons retain colour and open their achievement. Remove literal Locked/Unlocked labels. Current AchievementsView explicitly disables VisibilityPanel for all rows and adds status prefixes, matching the reported mismatch. Use icon-only desaturation rather than washing out text or disabling taps.

Acceptance checks for the next change: smooth 100–200 ms transitions; two-second countdown; rapid/mismatched/matched Pairs selections without whole-board dimming; Flash before/during/after recall; edge-to-edge backgrounds with controls clear of camera and home indicator; locked/unlocked achievement rows and tap details.
